## code to prepare `data_menages` dataset goes here

data <- capitulation::prepare_data(drawK = TRUE)

# fst::write_fst(data, path = "./inst/prepared_data.fst")
# fst::write_fst(data, path = "D:/W3CRK9/Mes Documents/PROJECTS/Microsimulation_archives/temporary/prepared_data.fst")
